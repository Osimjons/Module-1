'use strict';
for(let n = 2 ; n <= 10; ++n){
  console.log('/////////////////////////////////');
  for (let y = 2; y <= 10; ++y) {
    console.log(`${n} в ${y} в степени будет ${n ** y}`);
    
  }
};
